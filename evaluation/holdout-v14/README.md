# Holdout v14

Final strict post-review holdout. Twelve unseen companies (four per company type)
were selected from A-grade non-recruiting pre-cutoff operating events. MiniMax sees
only anonymized packets. Labels are searched only after prediction freeze under
the structured `uniform-director-plus-v1` protocol and validated against the
actual Director+ jobs inside `[2026-04-01, 2026-07-01)`.
