# Strict holdout v3

Eight entirely new companies were selected by a separate agent that was
prohibited from job searches and model-output access. Unlike v2, every company
is triggered by a non-funding operational event. Future labels remain sealed
until the MiniMax snapshot is written and hashed.