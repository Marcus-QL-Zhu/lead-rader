# Holdout V16

V16 is an independent blinded replay. It makes one pre-registered change from
V15: the model must return at most three concrete Director+ hypotheses per
company. The company universe is new. All inputs are pre-cutoff, A-grade,
non-recruiting events; JOSINT and job advertisements are excluded.

The future-label search may begin only after `pre-label-seal.json` exists.
